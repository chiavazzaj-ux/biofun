# Biofun Web (React + Vite + Tailwind)

Sitio de acompañamiento profesional para pequeños productores de hongos.

## 🚀 Deploy en Vercel
1. Subí esta carpeta a un repositorio **público** en GitHub.
2. En Vercel: **New Project → Import from GitHub → Deploy**.
3. En *Settings → Domains*, agregá `biofun.com.ar` y seguí las instrucciones DNS (delegación a `ns1.vercel-dns.com` y `ns2.vercel-dns.com`, o registros A/CNAME).

## ▶️ Desarrollo local (opcional)
```bash
npm install
npm run dev
```
Abrí http://localhost:5173

